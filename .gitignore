# Dependencies
node_modules/

# Build output (Vercel builds this itself — don't commit)
dist/

# Throwaway feasibility spikes (not production code)
spike/

# Local env files (never commit actual keys)
.env
.env.local
.env.*.local
/env

# Editor / OS
.DS_Store
*.local

# npm lockfile (project uses bun — keep bun.lock only)
package-lock.json

# gstack
.claude/skills/gstack/
.gstack/
.vercel
.env*
