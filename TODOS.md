# Instagram Competitor Finder — Implementation TODO List

Generated by `/autoplan` on 2026-05-26. Plan approved as-is (all 5 User Challenges resolved, cross-model consensus).

> **Build order is mandatory.** Pre-Layer-1 gates must pass before writing Layer 1. Layer 1 binary gate must pass before writing Layer 2. Layer 2 binary gate must pass before writing Layer 3 (React UI).

---

## 🔴 Pre-Layer-1 Gates (blocker — do first)

These two gates must both pass before writing a single line of `src/` code.

- [ ] **PRE-L1-1** · CORS verification — make a real `fetch()` call to `https://api.apify.com/v2/acts/{actorId}/runs` from a browser origin and confirm no CORS block. If blocked, the entire architecture must shift to a server proxy before proceeding. Document result in a `cors-verification.md` file.
- [ ] **PRE-L1-2** · ~~Confirm Apify actor IDs~~ **CONFIRMED: `apify/instagram-profile-scraper` only** (138K users, 99.7% success). No hashtag scraper needed — `relatedProfiles[10]` in Profile Scraper output gives better niche-adjacent candidates than hashtag search. Write actor ID into `src/lib/actors.ts`. Verify at least one run succeeds end-to-end (start run → poll → dataset fetch).

---

## 🟠 Layer 1 — Data & Key Infrastructure

Scripts-first: these modules must run in Node.js test scripts _and_ the browser. The binary gate (`scripts/test-scraper.mjs`) must return exit code 0 before Layer 2 begins.

- [ ] **TE9** · `src/lib/storage.ts` — cross-runtime storage adapter. Returns `window.localStorage` in browser, an in-memory `Map` in Node. All other Layer 1 modules import storage from here (never `window.localStorage` directly).
- [ ] **TE2** · `src/lib/keyRotator.ts` — round-robin Apify key selection with cooldown tracking. Uses storage adapter. `isReady()` returns true only when Gemini key exists AND at least one Apify key is not in cooldown. Cooldown timestamps stored as epoch milliseconds (numbers, not Date objects). Read-then-write is synchronous to prevent TOCTOU race.
- [ ] **TE1** · `src/lib/apifyClient.ts` — **2-round scrape workflow** (confirmed from real data):
  - **Round 1**: POST `apify/instagram-profile-scraper` with `{ usernames: inputHandles }` → poll → get profiles → extract `relatedHandles` from each profile
  - **Round 2**: Deduplicate + filter (remove input handles) → POST `apify/instagram-profile-scraper` with `{ usernames: candidateHandles }` → poll → get full candidate profiles
  - Total Apify actor runs: 2 (not 1). Both are async; each needs polling.
  - Input body: `{ "startUrls": [{ "url": "https://www.instagram.com/{handle}" }] }` OR `{ "usernames": ["handle1", "handle2"] }` — verify which format the actor accepts during PRE-L1-2.
- [ ] **TE13** · **P0 — Apify async polling** — `src/lib/apifyClient.ts` (extend TE1). After obtaining `runId`, poll `GET /v2/acts/{actorId}/runs/{runId}` every 2 seconds until `status === "SUCCEEDED"`, then fetch `GET /v2/datasets/{defaultDatasetId}/items`. Use `pLimit(3)` to cap concurrent actor runs. Timeout after 110 seconds (leave 10s buffer for the 120s hard limit).
- [ ] **TE5** · `src/lib/transformers.ts` — confirmed field map from real Apify output:
  - `followersCount` → followers (guard: < 100 → ER = null)
  - `latestPosts[*].likesCount` → avg likes (12 posts scraped by default)
  - `latestPosts[*].commentsCount` → avg comments
  - **ER outlier guard**: exclude posts where `commentsCount > followersCount * 0.05` before averaging (DM-bait posts like "Comment 'Details'" inflate comments; real data shows 6,335 comments on a 101K-follower account in one post)
  - `relatedProfiles[*].username` → `relatedHandles[]` (for Round 2 scraping)
  - ER formula: `(avgLikes + avgComments) / followers * 100`
  - Result type: `NormalizedProfile` (username, fullName, biography, followersCount, profilePicUrl, verified, postsCount, isBusinessAccount, avgLikes, avgComments, engagementRate, relatedHandles)
- [ ] **TE7** · `src/lib/actors.ts` — **single actor, confirmed from real data.** Export:
  ```typescript
  export const ACTORS = {
    PROFILE_SCRAPER: 'apify/instagram-profile-scraper',
  } as const
  // Input shape: { usernames: string[] }
  // Output shape: ProfileApifyRaw[] (see transformers.ts for field map)
  ```
- [ ] **TE8** · `scripts/test-scraper.mjs` — Layer 1 binary gate. Runs in Node. Tests: CORS headers reachable, actor run starts, polling reaches SUCCEEDED, dataset fetch returns valid shape, ER formula returns non-null for valid input. Exit 0 = pass, exit 1 = block Layer 2.

---

## 🟡 Layer 2 — AI Processing

AI modules depend on Layer 1 transformers and the COMPETITOR_CATEGORIES config. The binary gate (`scripts/test-ai.mjs`) must return exit code 0 before Layer 3 begins.

- [ ] **TE11** · `src/shared/utils/categories.js` — `COMPETITOR_CATEGORIES` config object. Keys: internal IDs. Values: `{ label: string, uiLabel: string, taxonomy: string[] }`. This is the single source of truth for both UI labels and AI taxonomy language.
- [ ] **TE10** · `src/ai/prompts.js` — inject `COMPETITOR_CATEGORIES` at runtime into prompt templates. Category names are template literals, not hardcoded strings. Changing `categories.js` changes AI output and UI labels in a single edit.
- [ ] **TE6** · `src/ai/gemini.ts` — Gemini REST API caller (no SDK). Handle 5 distinct error modes:
  - `429 RESOURCE_EXHAUSTED` → retry with exponential backoff (max 3 attempts)
  - `400 INVALID_ARGUMENT` → surface prompt error, do not retry
  - `500 INTERNAL` → surface transient error, suggest retry
  - `503 UNAVAILABLE` → surface service unavailable
  - Empty `candidates` array → SAFETY block — surface as content policy error
- [ ] **TE12** · `scripts/test-ai.mjs` — Layer 2 binary gate. Runs in Node. Tests: prompt renders with correct taxonomy, Gemini returns valid JSON shape (10 handles, categories, rationale), error modes return correct error types. Exit 0 = pass, exit 1 = block Layer 3.

---

## 🟢 Layer 3 — React UI

React UI is written only after both binary gates have passed. Build pages in this order: Settings → Input → Progress → Results.

### Store

- [ ] **TE4** · `src/store/keysStore.ts` — Zustand store with `persist`. Subscribes to `storage` events for cross-tab sync (other tabs changing keys triggers a re-read). Cooldown timestamps as epoch milliseconds. `isReady()` selector derived from `keyRotator.isReady()`.
- [ ] **T5** (from CEO review) · `analysisStore.ts` — Zustand store for analysis state (idle / running / done / error). Tracks current step (1–5), competitors array, error message.

### Design System

- [ ] **TD1** · `src/shared/styles/tokens.css` — CSS custom properties:
  - Colors: slate-50/100/200/700/900 for surfaces and text
  - `--color-accent: #4f46e5` (indigo-600)
  - `--color-badge-top-bg: #dbeafe` / `--color-badge-top-text: #1d4ed8` (blue-100/700)
  - `--color-badge-trending-bg: #fef3c7` / `--color-badge-trending-text: #b45309` (amber-100/700)
  - `--font-sans: 'Inter', sans-serif`

### Layout & Navigation

- [ ] **TD2** · `src/components/AppLayout.tsx` — persistent top bar: product title (left), Settings icon link (right). `<Outlet />` for page content. Min-width 1024px.

### Pages

- [ ] **TD3** · `src/pages/InputPage.tsx` — form order: handles textarea (required, 2–3 handles) → depth toggle (Standard / Deep) → client name input (optional) → Analyze CTA button. CTA disabled when `!isReady()`. First-time empty state: amber banner "Add your API keys in Settings to start analyzing."
- [ ] **TD7** · First-time empty state amber banner (component extracted from InputPage, reusable).
- [ ] **TE3** · `src/hooks/useCompetitorAnalysis.ts` — TanStack Query mutation with: `staleTime: 10 * 60 * 1000`, `gcTime: 30 * 60 * 1000`, `retry: 0`, `AbortController` signal wired to 120-second timeout. Exposes `mutate`, `isPending`, `data`, `error`.
- [ ] **TD5** · `src/components/ProgressSteps.tsx` — 5-step indicator with exact copy:
  1. "Scraping reference accounts"
  2. "Identifying content niche"
  3. "Discovering competitors by hashtag"
  4. "Ranking by engagement and growth"
  5. "Generating AI rationale"
- [ ] **TD6** · `src/components/CompetitorCard.tsx` — anatomy:
  - Avatar 48px circle (left)
  - `@handle` bold, follower count secondary
  - ER% prominent: amber if below cohort average, green if above
  - Category badge top-right: blue "Top" or amber "Trending"
  - Rationale paragraph (AI text)
  - 1px border, no box shadow
- [ ] **TD9** · ER coloring logic — compute cohort average ER from all 10 results; color each card relative to average (not absolute threshold).
- [ ] **TD8** · `src/pages/ResultsPage.tsx` — page hierarchy:
  1. Confidence/quality header (source handle count, scrape depth)
  2. "Top 5 — Established Authority" section (2-column grid ≥1280px, 1-col below)
  3. "Trending 5 — Growing Accounts" section (same grid)
  4. Sticky export bar (fixed bottom)
- [ ] **TD4** · Sticky export bar — two buttons: "Copy for Slides" (clipboard, formatted text: handle + ER% + rationale per line) + "Export CSV" (download, columns: rank, handle, followers, er, category, rationale, source_handles). Visible only when results present.
- [ ] **TD10** · "New Analysis" button on ResultsPage — clears analysis store, navigates to InputPage.
- [ ] **TD11** · Navigation persistence — Settings link always visible in AppLayout top bar (not hidden on Results page).
- [ ] **T1** · `src/pages/SettingsPage.tsx` — Gemini API key input + up to 10 Apify key inputs. Save validates keys (test API call). Shows which keys are in cooldown + time remaining.
- [ ] **T2** · Key validation on save — lightweight test call (Gemini: models list endpoint; Apify: account endpoint).
- [ ] **T3** · Depth toggle wired to actual scraper config (Standard = 5 hashtags, Deep = 8 hashtags).
- [ ] **T4** · Client name field — optional, used as prefix in CSV export filename.
- [ ] **T6** · Cooldown indicator — per-key display on Settings page. Shows status (active / cooldown), time remaining if in cooldown.
- [ ] **T7** · ER formula exposed in UI (tooltip or info icon explaining formula: `(likes + comments) / followers × 100`).
- [ ] **T8** · Results sort: Top 5 by follower count desc, Trending 5 by ER desc.
- [ ] **T9** · 120-second AbortController timeout wired through TQ mutation → apifyClient → all pending fetches aborted on timeout.
- [ ] **T10** · Error state UI — shows which of the 5 steps failed, which Apify key was in use, actionable message.

---

## 🔵 Integration & QA

- [ ] Run `scripts/test-scraper.mjs` end-to-end with real keys (Layer 1 gate — must exit 0)
- [ ] Run `scripts/test-ai.mjs` end-to-end with real keys (Layer 2 gate — must exit 0)
- [ ] Full app flow: Settings → Input (2 handles) → Progress (watch step indicators) → Results (Top 5 / Trending 5 visible, export works)
- [ ] Cross-tab test: open two tabs, add key in Tab 1, confirm Tab 2 keysStore updates without refresh
- [ ] Error paths: invalid Gemini key, all Apify keys in cooldown, Apify 429, Gemini SAFETY block, 120s timeout
- [ ] Export test: Copy for Slides (confirm clipboard content), Export CSV (confirm download + column headers)
- [ ] Taxonomy test: change a category name in `categories.js`, confirm AI prompt changes + UI label changes

---

## 🟠 P1 Deferred — Conversational UX Polish

Captured during `/ship` review (2026-06-01). Deferred from PLAN.md per plan completion audit.

- [x] **AD1 — "Quick picks:" label above confirm buttons** · `ChatOptions` accepts a `label` prop; `ChatMessage` passes `"Quick picks:"` when `!optionsDisabled`. **Completed:** v0.3.0 (2026-06-01)
- [x] **AD5 — Retry counter + escalation message** · `useConversation` tracks `confirmErrorCount`. After 2 failures message escalates to "Let's keep it simple…" (error type), `isConfirmingLocked` exposed; `ChatPage` locks textarea + updates placeholder. Counter resets on button click. **Completed:** v0.3.0 (2026-06-01)
- [x] **AD10 — Auto-focus textarea on confirming entry** · `useEffect` in `ChatPage` watches `status` and calls `textareaRef.current?.focus()` on transition to `'confirming'`. **Completed:** v0.3.0 (2026-06-01)
- [x] **Integration tests for confirming path** · `src/hooks/useConversation.confirming.test.ts` — 7 tests (RTL + jsdom): empty text no-op, heuristic match bypasses Gemini, Gemini fallback called, `isConfirmingPending` reset in finally, AD5 first/second failure escalation, button-click counter reset. **Completed:** v0.3.0 (2026-06-01)

---

## 🟡 P2 Deferred — Discovery Polish

Captured during `/ship` review (2026-05-29). Not blocking current PR.

- [ ] **D1.6 — Discovery progress steps 3+4 flash instantly** · Steps 3 ("Profile scrape") and 4 ("Location filter") fire back-to-back after `runLocationDiscovery()` returns, so users see them flash without a pause. Fix: advance step markers inside the pipeline callbacks (progress events) rather than after the promise resolves. Effort: S (~20 min).
- [ ] **D1.7 — `useCompetitorAnalysis` double-instance in ProgressPage** · `ProgressPage` creates a second `useMutation` instance via `useCompetitorAnalysis()` (for `answerClarification` + `isPending`) separate from the instance created in `InputPage`. TQ mutations are instance-scoped — the two instances don't share state, so `isPending` in ProgressPage reflects the ProgressPage mutation instance (always false) rather than the InputPage one. Fix: lift the mutation to a shared React context or derive `isPending` from `analysisStore.status === 'running'`. Effort: M (~45 min).

---

## 🔮 Backlog — Post-Niche-Signal Approach A

These items are approved for a future iteration, after the niche-signal enrichment (Approach A) ships and baseline quality is confirmed.

- [ ] **APPROACH-B** · Multi-hop competitor discovery (Round 3 scrape) — after Round 2 completes, extract `relatedProfiles` from the *candidate* profiles (not just the input profiles) and run a Round 3 scrape on those handles. This expands the candidate pool from ~20 to ~60 accounts without requiring a new Apify actor. Effort: M (~60 min). Risk: Med (3 Apify rounds may push toward 120s timeout — profile before shipping). Gate: Approach A ships first and confirms that signal quality, not pool size, was the binding constraint. If 5+/10 is still not met after A, this becomes P1.

---

## 🚫 Deferred — v2

These items were raised in reviews and explicitly deferred. Do not implement in v1.

- Shared backend key store (team-wide, not per-browser)
- Multi-user / team workspace
- Historical analysis tracking and comparison
- Competitor trend graphs (30-day ER change visualization)
- Mobile responsive layout (v1 is desktop-only, min 1024px)
- Webhook or Slack notifications on analysis complete
- Automated scheduled analysis
- PDF export
- White-label report generation
- Batch analysis (multiple client sets)

---

## 📋 Dependency Map (build order enforced)

```
PRE-L1-1 (CORS) ──┐
PRE-L1-2 (actors) ─┤
                   ↓
TE9 (storage) → TE2 (keyRotator) → TE1 (apifyClient) → TE13 (polling)
                                 → TE5 (transformers)
                                 → TE7 (actors)
                                 → TE8 (test-scraper gate) ─── BINARY GATE 1
                                                                      ↓
                                   TE11 (categories) → TE10 (prompts)
                                                     → TE6 (gemini)
                                                     → TE12 (test-ai gate) ─── BINARY GATE 2
                                                                                      ↓
TE4 (keysStore) → TD1 (tokens) → TD2 (AppLayout) → SettingsPage → InputPage
                                                                 → ProgressSteps
                                                                 → CompetitorCard → ResultsPage
                                                                                  → Export bar
```

---

_38 tasks total (12 CEO-phase + 2 pre-L1 gates + 11 design-phase + 13 eng-phase). All User Challenges closed. Plan approved 2026-05-26._

---

## Future: Client Handoff

- [ ] **UI rendering tests** — Add React Testing Library tests for `ResultsPage` provenance line (`candidateCount` interpolation) and confirming message format. Deferred: internal pre-call tool; manual QA is sufficient until the tool is handed off to clients. Context: store tests (T6 in UX Transparency Layer) cover data path but not JSX rendering. Prerequisite: React Testing Library + mock store setup (~1h one-time). Triggered by: `plan-eng-review` D7 / outside voice finding.

---

## Reel Analysis — Deferred (added by /plan-eng-review 2026-06-01)

- [ ] **REEL-CACHE-1** · `localStorage` cache keyed by `{handle}-{date}` with 24h TTL for reel analysis results.
  - **Why:** Without caching, every page refresh re-runs all Apify scrapes (90-180s per creator) and all Gemini calls (50 calls per 5-creator run = 10+ min total). A 24h cache turns a repeat visit into an instant load.
  - **How:** On `ReelAnalysisPage` mount, check `localStorage.getItem('reel-analysis-{handle}-{YYYY-MM-DD}')` before firing Apify. If hit, inject into store directly and skip scrape + Gemini analysis. On completion, write `ReelData[]` + `ReelAnalysis[]` to the same key with a timestamp.
  - **Scope:** `reelScraper.ts` for the cache read/write wrapper; `ReelAnalysisPage.tsx` for cache-aware mount logic.
  - **Blocked by:** v1 feature shipped.
  - **Priority:** High — cost and time per run make caching the highest-leverage follow-up.

---

## Content Copilot Overhaul — Deferred (added by /ship 2026-06-02, v0.4.0.0)

Shipped in v0.4.0.0: green baseline + security hardening, reel/hooks as an independent NL tool, the content copilot, and deeper HookMap analysis. The full repo audit lives in `.planning/audit-findings.md` (42 findings: 3 Critical + 11 High + 16 Medium + 12 Low). Criticals C1–C3 + H9/H11 + the build/lint baseline shipped in this release. Remaining, deferred:

**Closed in v0.4.0.0** (cleanup increment): AUDIT-H8 (business filter now matches the city in `fullName` instead of a dead `businessAddress` branch), AUDIT-H10 (Gemini auth errors surface instead of silent template fallback), AUDIT-M7 (`JSON.stringify` escaping + structural-only retry note), AUDIT-M10 (persisted-index key rotation), plus M3 (abort → `ABORTED` in `pollRun`), M8 (abort-aware Gemini backoff), M9 (discovery-error effect deps), M13 (stable monotonic message keys).

Still open:

- [ ] **AUDIT-H6** · Orphaned `/results` + `/discover/results` pages — results render inline now, nothing navigates there. Decide: wire navigation or delete them + the `resultsPath` registry field. **Priority: Medium.**
- [ ] **AUDIT-M1 / M2** · `useConversation` multiplexes one `discoveryAbortRef` across parse/discovery/confirm/follow-up and shares nudge/timeout refs — a rapid second send can abort the wrong request or orphan a timer. Give each concern its own ref. **Priority: Medium.**
- [ ] **AUDIT-M11 / M12 / M14 / M15** · Clarification fallback copy divergence (M11); `confirmErrorCount` lock has no auto-recovery (M12); pipeline-switch recursion has no depth guard (M14); discovery final pool slices unsorted lists (M15). **Priority: Medium/Low.**
- [ ] **SHIP-P3** · After a reel run, status returns to `'chatting'`, so a typed follow-up re-runs intent parsing rather than the content copilot. The "remix" button is the intended grounded handoff, and a content-classified follow-up still gets reel grounding — so minor. **Priority: Low.**
- [ ] **AUDIT-DOCS** · `TODOS.md` (this file), `AGENTS.md`, `CLAUDE.md` reference deleted `InputPage`/`ProgressPage` and the abandoned indigo/Inter/slate design system. Prune stale references. **Priority: Low.**
- [ ] **AUDIT-L10 / L12** · Desktop-only `body { min-width: 1024px }` (document the intent or add a small-screen message); `package.json` name is still `instagram-competitor-finder` not `content-os`. **Priority: Low.**
