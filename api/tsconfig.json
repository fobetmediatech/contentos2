{
  "//": "Vercel serverless functions (ESM Node). Separate from the app's bundler-mode tsconfig: node types for process, DOM lib for fetch/Request/Response/BodyInit, nodenext so .js import extensions resolve. Not referenced by the app's tsc -b.",
  "compilerOptions": {
    "target": "ES2023",
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "lib": ["ES2023", "DOM"],
    "types": ["node"],
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "verbatimModuleSyntax": true,
    "esModuleInterop": true,
    "resolveJsonModule": true
  },
  "include": ["**/*.ts"],
  "exclude": ["**/*.test.ts", "**/*.eval.test.ts"]
}
